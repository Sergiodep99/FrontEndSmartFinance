import { MatchPasswordDirective } from './match-password.directive';

describe('MatchPasswordDirective', () => {
  it('should create an instance', () => {
    expect(new MatchPasswordDirective()).toBeTruthy();
  });
});
